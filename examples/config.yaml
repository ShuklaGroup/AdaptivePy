# Example AdaptivePy configuration (feature-only run)

features_dir: examples/data/features
output_dir: examples/results

clustering:
  method: kmeans
  n_clusters: 5
  params: {}

policies:
  - least_counts
  - random
  # - fast

# Example FAST policy settings (uncomment with policies: [fast])
# policy_params:
#   fast:
#     feature_indices: [0]
#     directions: [maximize]
#     alpha: 1.0

# Example MA-REAP policy settings (uncomment with policies: [ma_reap])
# policy_params:
#   ma_reap:
#     n_candidates: 6
#     agents:
#       agent_0: [traj_0]
#       agent_1: [traj_1]
#     delta: 0.05
#     regime: collaborative

# Example MaxEnt VAMPNet settings (requires pip install adaptivepy-sampling[torch])
# policies:
#   - maxent_vampnet
# policy_params:
#   maxent_vampnet:
#     output_states: 8
#     lagtime: 10
#     epochs: 100
#     batch_size: 2048

# Example TS-DAR settings (requires pip install adaptivepy-sampling[torch])
# policies:
#   - ts_dar
# policy_params:
#   ts_dar:
#     n_states: 4
#     latent_dim: 3
#     encoder_sizes: [4, 128, 64, 3]
#     lagtime: 10
#     epochs: 100
#     batch_size: 2048

n_seeds: 3

seed_selection:
  method: nearest_center

random_seed: 42
write_pdbs: false
